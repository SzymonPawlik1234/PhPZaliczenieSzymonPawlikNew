{{ form_start(form) }}
    {{form_row(form.typ)}}
    {{form_row(form.marka)}}
    {{form_row(form.model)}}
    {{form_row(form.cena)}}
    {{form_row(form.brzmienie)}}

    {{ form_row(form.save , {'label': 'Zapisz'}) }}

{{form_end(form)}}

